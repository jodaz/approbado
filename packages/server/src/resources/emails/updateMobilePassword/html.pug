div.content
    h3(style="font-family: 'Segoe UI'") Cambio de contraseña
    p(style={
        'text-align': 'center',
        'font-family': 'Segoe UI'
    })=`Hola ${name}, para cambiar la contraseña debe copiar el siguiente código: ${token} y pergarlo en el campo código en la App`
